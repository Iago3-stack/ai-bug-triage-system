# Sem bloco [theme]: um tema custom único trava/oculta o seletor Claro/Escuro/Sistema
# no menu "⋮" do Streamlit (a opção só aparece quando há 2+ temas disponíveis).

[browser]
gatherUsageStats = false
